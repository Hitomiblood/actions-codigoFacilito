name: Deploy to Server

on: push

jobs:
  first_jobs:
    name: Tutorial CódigoFacilito
    runs-on: ubuntu-latest
    outputs:
      my_variable: ${{ steps.step_1.outputs.my_variable }}
    env:
      NAME: CodigoFacilito

    steps:
      - name: working directory
        id: step_1
        run: echo "my_variable=HolaMundo" >> $GITHUB_OUTPUT

      - name: Obtener Mensaje
        run: echo "${{ steps.step_1.outputs.my_variable }}"

  second_jobs:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first_jobs
    runs-on: ubuntu-latest

    steps:
    - name: Hola Mundo 2
      run: echo "Hola Mundo 2"
    - name: verificando variable
      run: echo "La variable es ${{ needs.first_jobs.outputs.my_variable }}"
