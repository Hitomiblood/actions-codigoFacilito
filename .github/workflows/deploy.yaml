name: Deploy to Server

on: push

jobs:
  first_jobs:
    name: Tutorial CódigoFacilito
    runs-on: ubuntu-latest
    env:
      NAME: CodigoFacilito

    steps:
      - name: working directory
        run: echo "my_variable=HolaMundo" >> $GITHUB_OUTPUT


  second_jobs:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first_jobs
    runs-on: ubuntu-latest

    steps:
    - name: Hola Mundo 2
      run: echo "Hola Mundo 2"
