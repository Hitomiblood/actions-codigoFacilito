name: Deploy to Server

on: push

jobs:
  first_jobs:
    runs-on: ubuntu-latest
    env:
      NAME: CodigoFacilito
    steps:
    - name: Hola Mundo
      run: echo "Hola Mundo"
      
  second_jobs:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first_jobs
    runs-on: ubuntu-latest

    steps:
    - name: Hola Mundo 2
      run: echo "Hola Mundo 2 $NAME"
